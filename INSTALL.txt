SecuredLanding Approval / Generated Document Status Fix
=======================================================

1. Replace:
   src/pages/AdminDashboard.tsx

2. Commit and redeploy.

This package builds on the previous accepted-revised-offer approval fix.

Changes:
- Approval-created generated loan documents use `pending` instead of
  `ready_for_review`, because the production database rejected
  `ready_for_review` under generated_loan_documents_status_check.
- The stale revised-offer heading no longer says
  "Administrator response required"; it displays "Revised offer accepted".
- Previous stale revision-status approval fix is retained.
- Previous property-save verification is retained.

If the database still rejects `pending`, run DIAGNOSTIC.sql in Supabase and
send the result. That will show the exact status values permitted by the
production CHECK constraint so the app can be matched exactly.
