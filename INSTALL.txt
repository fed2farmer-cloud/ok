SECUREDLANDING V3.2 — COUNTEROFFER PRODUCTION-READY PATCH

1. Copy src/components/MessageCenter.tsx into your project.
2. Run supabase/migrations/20260729_counteroffer_production_ready.sql in Supabase SQL Editor.
3. Deploy the project.
4. Sign in as a borrower and test Accept and Decline.

Important:
- The SQL records a timestamp, authenticated borrower ID, agreement version, and browser user-agent.
- It does not claim to capture a reliable client IP address. Capture IP through a trusted server/Edge Function if legally required.
