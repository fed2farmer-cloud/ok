SecuredLanding v2.9 Counteroffer + Revision Fix

Replace this file in your project:

src/components/AdminLoanReviewActions.tsx

Fixes included:
- Uses bigint-compatible numeric loan_application_id values.
- Uses the actual loan_counteroffers columns:
  original_loan_amount, proposed_loan_amount, resulting_ltv_percent,
  explanation, estimated_monthly_payment, maximum_ltv_percent.
- Uses the actual loan_revision_requests revision_items column.
- Sends borrower_user_id and borrower notification user_id.
- Uses valid workflow statuses for the new history tables.
- Displays the real Supabase error message instead of only a generic alert.

Database prerequisite:
The corrected bigint migration must already be applied. Your SQL screenshots
confirm that loan_applications.id and both workflow foreign keys are bigint.

Validation:
npm run build completed successfully.
