import {useEffect,useState} from "react"; import AppLayout from "../components/AppLayout"; import {supabase} from "../lib/supabase";
export default function SecondaryMarket(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase.from('secondary_market_listings').select('*').in('status',['open','partial']).order('created_at',{ascending:false}).then(({data})=>setRows(data||[]))},[]);return <AppLayout><div className="mx-auto max-w-6xl p-6"><h1 className="text-3xl font-black">Secondary Market</h1><p className="mt-2 text-slate-600">Test-mode resale listings. Trading remains feature-gated until compliance and settlement controls are approved.</p><div className="mt-6 grid gap-4 md:grid-cols-2">{rows.map(x=><div className="rounded-2xl border bg-white p-5" key={x.id}><div className="text-sm font-bold text-slate-500">Listing #{x.listing_number}</div><h2 className="text-xl font-black">Loan #{x.loan_number}</h2><p className="mt-2">Principal available: ${Number(x.principal_remaining).toFixed(2)}</p><p>Asking price: ${Number(x.asking_price).toFixed(2)}</p><button disabled className="mt-4 rounded-xl bg-slate-300 px-4 py-2 font-bold">Trading disabled in test foundation</button></div>)}</div>{rows.length===0&&<div className="mt-6 rounded-2xl border bg-white p-6">No active listings.</div>}</div></AppLayout>}
