/**
 * Utility functions shared between wallet payment components (Apple Pay, Google Pay)
 */
/**
 * Get the user's browser language
 * @param fullLocale Whether to return the full locale (e.g., 'en-US') or just the language code (e.g., 'en')
 * @returns The browser language code
 */
export declare function getBrowserLanguage(fullLocale?: boolean): string;
/**
 * Determine if a color is light or dark
 * @param color The color to check (hex or rgb)
 * @returns True if the color is light, false if dark
 */
export declare function isLightColor(color: string): boolean;
/**
 * Determine the best button color based on background
 * @param themeStyles The theme styles object containing background color
 * @returns 'black' or 'white' depending on background color
 */
export declare function determineButtonColor(themeStyles: Record<string, string>): 'black' | 'white';
