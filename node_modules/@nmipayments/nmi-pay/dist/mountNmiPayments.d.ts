import { PaymentWidgetProps, ExpressCheckoutConfig } from './types';
export interface MountedNmiPayments {
    element: HTMLElement;
    destroy: () => void;
    resetFields: () => void;
    updateExpressCheckoutConfig: (config: Partial<ExpressCheckoutConfig>) => void;
}
/**
 * Safely mounts an NMI Pay widget to a target element.
 *
 * The custom element will be automatically registered if not already available.
 * The NMI Gateway script is automatically bundled and loaded with the component.
 *
 * @param targetElement - DOM element or CSS selector where to mount the widget
 * @param options - Payment widget configuration options
 * @returns Control object with methods to interact with the mounted widget
 *
 * @example
 * ```typescript
 * import { mountNmiPayments } from '@nmipayments/nmi-pay';
 *
 * const widget = mountNmiPayments('#payment-container', {
 *   tokenizationKey: 'your-key',
 *   layout: 'multiLine',
 *   paymentMethods: ['card', 'ach'],
 *   onChange: (data) => console.log('Payment change:', data)
 * });
 *
 * // Reset the payment form
 * widget.resetFields();
 *
 * // Update Google Pay amount
 * widget.updateGooglePayOptions({ amount: '25.99' });
 *
 * // Later, to destroy
 * widget.destroy();
 * ```
 */
export declare function mountNmiPayments(targetElement: HTMLElement | string, options: PaymentWidgetProps): MountedNmiPayments;
