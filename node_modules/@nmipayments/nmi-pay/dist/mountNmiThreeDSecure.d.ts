import { NmiThreeDSecureProps, PaymentInformation, KountSessionCompleteEvent, KountSessionFailureEvent } from './types';
export interface MountedNmiThreeDSecure {
    element: HTMLElement;
    destroy: () => void;
    startThreeDSecure: (paymentInfo: PaymentInformation) => void;
    startKountSession: () => Promise<KountSessionCompleteEvent | KountSessionFailureEvent>;
}
/**
 * Safely mounts an NMI 3D Secure widget to a target element.
 *
 * The custom element will be automatically registered if not already available.
 * The Gateway.js script is automatically loaded when needed.
 *
 * @param targetElement - DOM element or CSS selector where to mount the widget
 * @param options - 3D Secure widget configuration options
 * @returns Control object with methods to interact with the mounted widget
 *
 * @example
 * ```typescript
 * import { mountNmiThreeDSecure } from '@nmipayments/nmi-pay';
 *
 * const widget = mountNmiThreeDSecure('#threeds-container', {
 *   tokenizationKey: 'your-key',
 *   modal: true,
 *   onComplete: (data) => console.log('3DS complete:', data),
 *   onFailure: (error) => console.log('3DS failed:', error)
 * });
 *
 * // Start 3D Secure authentication
 * widget.startThreeDSecure({
 *   paymentToken: 'token_123',
 *   amount: '100.00',
 *   email: 'user@example.com'
 * });
 *
 * // Later, to destroy
 * widget.destroy();
 * ```
 */
export declare function mountNmiThreeDSecure(targetElement: HTMLElement | string, options: NmiThreeDSecureProps): MountedNmiThreeDSecure;
